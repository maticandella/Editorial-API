import GenericRepository from '../genericRepository.js';

export default class SocialMediaTypeRepository extends GenericRepository {
    constructor(socialMediaTypeModel) {
        super(socialMediaTypeModel);
    }
}